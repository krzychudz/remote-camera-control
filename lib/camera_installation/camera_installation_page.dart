import 'package:app/camera_installation/camera_installtion_screen.dart';
import 'package:flutter/material.dart';

class CameraInstallationPage extends StatelessWidget {
  const CameraInstallationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CameraInstallationScreen();
  }
}
